export type Supplier = {
  id: number | null;
  email: string;
  name: string;
  vatId: string;
  city: string;
  country: string;
  street: string;
  postalCode: string;
  bankAccount: string;
  bankCode: string;
  iban: string;
}
